# Core dependencies for dire-rapids
numpy>=1.21.0
torch>=2.0.0
pykeops>=2.1.0
loguru>=0.6.0
tqdm>=4.62.0

# Scientific computing
scipy>=1.7.0
scikit-learn>=1.0.0
pandas>=1.3.0
plotly>=5.0.0
psutil>=5.8.0

# Betti curve distance metrics
fastdtw>=0.3.0

# Optional GPU acceleration (RAPIDS)
# Use the repository's rapids-cu12 or rapids-cu13 extra for the pinned RAPIDS
# 26.06 stack; do not add both. See docs/numpy2_rapids.md for the matching
# PyTorch wheel index and install order.


# Network dependencies (compatible with google-adk)
websockets>=10.3,<16.0.0
